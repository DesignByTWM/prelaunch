/**
 * Superseded by Services.tsx.
 *
 * The earlier 2+3 hierarchical grid was my own invention and did not match
 * the approved V2 design, which uses one equal-weight row. Kept as a
 * re-export so any stale import still resolves rather than failing the
 * type check. Safe to delete.
 */
export { Services as ServiceCards } from "@/components/home/Services";
